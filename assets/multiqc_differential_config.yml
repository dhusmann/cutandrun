report_comment: >
  Differential analysis report generated by nf-core/cutandrun. Method-specific
  metrics (e.g., ChIPBinner clustering parameters) are included in the summary table.

run_modules:
  - custom_content

custom_content:
  order:
    - differential_design
    - differential_summary

sp:
  custom_content:
    fn: "*_mqc.tsv"
